export class Metro {
    constructor(
        public idt: string,
        public linea: string,
        public numchatting: number, //n passeggeri in chat
        public tempo: number) //orario di partenza
    { }
}